{
  "spec_version": 1,
  "connector_type": "mgp_server",
  "id": "cpersona",
  "name": "CPersona",
  "description": "Persistent character persona memory with FTS5 + vector hybrid search, anti-contamination, and a 27-tool surface. Single SQLite file, zero LLM dependency at the storage layer.",
  "version": "2.4.35",
  "category": "memory",
  "trust_level": "core",
  "magic_seal": "sha256:270c1eb87fe5c3942b64936c54bf235d4eeeac4054a370fdd5093aa56778a706",
  "install": {
    "source": {
      "type": "git",
      "url": "https://github.com/Cloto-dev/cpersona",
      "reference": "",
      "subdir": null
    },
    "package_manager": "uv",
    "runtime": "python",
    "directory": "",
    "dependencies": [],
    "bin_name": null
  },
  "icon": "brain",
  "tags": ["memory", "persona", "embedding", "search"],
  "host_compatibility": [
    "clotocore",
    "claude-code",
    "claude-desktop",
    "cursor",
    "project-airi"
  ],
  "env_vars": [],
  "optional_env_vars": [
    {
      "name": "CPERSONA_DB_PATH",
      "default": "./cpersona.db",
      "description": "SQLite database path. Defaults to a relative path; set to an absolute path in production."
    },
    {
      "name": "EMBEDDING_MODE",
      "default": "none",
      "description": "Embedding mode (none | http | api). Generic key shared with other Cloto MCP servers. Falls back to CPERSONA_EMBEDDING_MODE for backward compatibility."
    },
    {
      "name": "EMBEDDING_HTTP_URL",
      "default": "http://127.0.0.1:8401/embed",
      "description": "HTTP-mode embedding endpoint. Required when EMBEDDING_MODE=http. Falls back to CPERSONA_EMBEDDING_URL."
    },
    {
      "name": "CPERSONA_RECALL_MODE",
      "default": "rrf",
      "description": "Recall strategy: 'rrf' (Reciprocal Rank Fusion across FTS / vector / keyword) or 'cascade' (sequential fallback)."
    },
    {
      "name": "CPERSONA_CONFIDENCE_ENABLED",
      "default": "false",
      "description": "Include confidence metadata in recall results."
    },
    {
      "name": "CPERSONA_AUTO_CALIBRATE",
      "default": "false",
      "description": "Auto-calibrate vector similarity threshold on startup using the null-distribution z-score method."
    },
    {
      "name": "CPERSONA_RECALL_PRECISION",
      "default": "balanced",
      "description": "Recall precision point (knob 3): 'strict' (fewer contaminants, more misses), 'balanced' (Youden's J), or 'lenient' (fewer misses, more contaminants). Shifts where the calibrated post-fusion gate sits on its data-derived curve. The curve is auto-calibrated; this is the one policy choice."
    }
  ],
  "auto_restart": true,
  "changelog": "v2.4.35: Code-quality patch batch — fixes the CRITICAL + HIGH findings from the v2.4.34 audit (registry bug-003..bug-008). (bug-003, CRITICAL) The Streamable-HTTP BearerTokenMiddleware let header-less requests fall through to the app even with CPERSONA_AUTH_TOKEN set (auth bypass on the default 0.0.0.0 bind); it now rejects any missing/blank/malformed token with 401 and compares tokens with hmac.compare_digest. (bug-004) Schema migrations previously swallowed every ALTER failure with a bare except and then stamped SCHEMA_VERSION unconditionally, so a transient failure (locked DB / disk I/O) left a column missing but the version marked current and never retried; migrations now use idempotent existence checks and the version stamp is withheld unless the ladder completes cleanly. (bug-005) A malformed task payload raised outside the loop's try, silently killing the queue worker; a poison row now discards and the loop advances, plus a done-callback surfaces abnormal loop exits. (bug-006) An empty-summary archive_episode enqueued a task the worker completed as a no-op (episode silently dropped while returning ok:true/queued:true); the wrapper now rejects it explicitly since server-side summarisation no longer exists. (bug-007) check_health(fix=true) duplicate-content repair ran a global DELETE ignoring agent scope and locked rows; it is now agent-scoped and never deletes a locked row. (bug-008) memories_fts had no AFTER UPDATE trigger, so in-place content edits left stale trigrams (old wording kept matching new content); adds the trigger and rebuilds the index once. SCHEMA_VERSION 10 -> 11 (adds the memories_fts_au trigger + one-time FTS rebuild, in-place). Tool count unchanged at 27. server.py changed, so the published seal's entry_point hash must be regenerated on the next hub publish.\nv2.4.34: PyPI packaging. Restructure the flat module layout into a proper cpersona/ package (server modules + the vendored mcp_common snapshot now live under cpersona/, with imports rewritten config -> cpersona.config etc.) so a wheel no longer installs top-level modules into site-packages — making the project safe to publish on PyPI. Adds a console-script entry (cpersona = cpersona.server:run) and cpersona/__main__.py, so the server runs via `uvx cpersona`, `pip install cpersona` + `cpersona`, or `python -m cpersona`. The repo root keeps a thin server.py shim that re-exports cpersona.server:run, so the git-clone / marketplace install path (`python server.py`) is unchanged; the shim is excluded from the wheel. Behavior-preserving (packaging only): no schema change (SCHEMA_VERSION stays 10), tool count unchanged at 27, no recall/storage behavior change. server.py content changed (now a shim), so the published seal's entry_point hash must be regenerated on the next hub publish.\nv2.4.33: Degraded-advisory runtime guard (the runtime companion to the install-time setup self-check). When embeddings are unavailable at runtime — mode=none (FTS-only by configuration), or a configured http endpoint becomes unreachable (process died, port changed, DB copied to a host without the embedding server, startup race) — recall previously degraded to keyword/FTS-only SILENTLY. do_recall now attaches an `advisory` field to its response so the calling agent can self-report the degradation to the user instead of quietly serving reduced recall. A process-level health state machine (new health.py: unknown/healthy/hint/fault, reset on restart) is fed by a measurement at the embedding boundary in vector.py: on an embed failure it runs a short non-swallowing health probe (the vendored EmbeddingClient.embed() returns None for both the configured-off and the endpoint-down cases, so the probe recovers the real transport error for the advisory's evidence) and promotes to fault only after two consecutive failures (single-blip debounce); hint (mode=none) is detected statically at recall entry. The advisory is a struct {degraded, severity, reason, evidence, runbook} carried on the recall response (MCP cannot push, so a fault surfaces on the next recall); the full runbook fires once per outage, then a short reminder, and recovery re-arms it. On by default; opt out with CPERSONA_DEGRADED_ADVISORY=false for a deliberate keyword-only deployment. server.py is unchanged, so the published seal's entry_point hash is identical to v2.4.32. No schema change (SCHEMA_VERSION stays 10); tool count unchanged at 27.\nv2.4.32: Complete the ''=global recall change in the vector search path (fix). v2.4.31 added the ''=global clause (a stored channel of '' matches every channel-scoped recall) to the FTS episode + memory-keyword paths in memory_handlers.py, but the vector (embedding) search path in vector.py was omitted from the v2.4.31 commit, so a vector-mode recall scoped to a concrete channel still missed channel='' memories (partial ''=global). This adds the same (channel = ? OR channel = '') clause to both vector.py sites: the remote by-id fetch and the local candidate scan. server.py is unchanged, so the published seal's entry_point hash is identical to v2.4.31. No schema change (SCHEMA_VERSION stays 10); tool count unchanged at 27.\nv2.4.31: Channel-axis migration + ''=global recall (knob2 v2 default-flip prep, Goal #120). New tool migrate_channel_axis(agent_id, dry_run=true, globalize_unrecoverable=false) re-channels bridge-type memories (channel='discord') to the concrete channel recovered from metadata.session_id ('{channel_id}:{user_id}:{chunk}' | '{channel_id}:shared' -> channel_id), so per-channel recall can match memories the kernel filed under the bridge type rather than the concrete channel. Non-destructive (only the channel column changes), idempotent (a no-op once a row is moved off 'discord'), dry-run by default; the report carries a recoverable bucket (grouped by recovered channel) and an unrecoverable bucket (channel='discord' rows with no snowflake session_id). globalize_unrecoverable=true moves the unrecoverable remnant to channel='' instead of leaving it stranded. Recall change: a stored channel of '' is now global -- it matches every channel-scoped recall (vector by-id + scan in vector.py, episode FTS and memory keyword in memory_handlers.py), so old/global memories and pre-per-channel episodes are not orphaned once recall filters by the concrete channel. list_memories now also returns each memory's channel (additive field) so the kernel can group unarchived memories per channel for per-channel episode archival (the kernel-side knob2 v2 follow-up). New tool changes the server entry point, so the published seal needs a fresh server.py hash. No schema change (SCHEMA_VERSION stays 10); tool count 26 -> 27.\nv2.4.30: Recall precision read-back (knob 3 follow-up, Goal #120). Adds get_recall_precision(agent_id), the read companion to set_recall_precision: returns an agent's effective specificity weight (beta) and named precision level (strict / balanced / lenient, or 'custom' for a raw beta override), plus whether the value is a per-agent override or the global CPERSONA_RECALL_PRECISION default (overridden + global_precision / global_beta). Read-only — it never recalibrates and never persists, so it is not gated by no-persist pause (like recall) — letting a client load the current setting, let the user edit it, and write it back instead of the control being write-only. New tool changes the server entry point, so the published seal needs a fresh server.py hash. No schema change (SCHEMA_VERSION stays 10); tool count 25 -> 26.\nv2.4.29: Per-agent recall precision (knob 3, Goal #120). The precision point (strict / balanced / lenient -> specificity weight beta of 2.0 / 1.0 / 0.5 in the gate separation objective) was global-only via CPERSONA_RECALL_PRECISION; it is now a per-agent override. Adds a per-agent beta store (vector._agent_betas) with a _get_precision_beta(agent_id) resolver that falls back to the global default, so the gate calibration sits each agent's post-fusion quality gate on the separation curve at that agent's beta. The override is persisted in the calibration sidecar (new agent_betas key) next to the gate it produced — the two are restored together so they never desync; a pre-2.4.29 sidecar with no key leaves every agent on the global default (backward compatible). New tool set_recall_precision(agent_id, precision, beta) sets or clears an agent's precision and recalibrates its gate at the new beta immediately (no restart) — precision is a per-agent setting, not a per-recall argument, because the gate threshold is precomputed at a fixed beta, so this recalibrates once instead of per recall. An empty precision with beta <= 0 clears the override. Un-configured agents still track CPERSONA_RECALL_PRECISION on their next calibration. New tool changes the server entry point, so the published seal needs a fresh server.py hash (the first such change since v2.4.25). No schema change (SCHEMA_VERSION stays 10); tool count 24 -> 25.\nv2.4.28: Hotfix — restore recall under CONFIDENCE_ENABLED. v2.4.27 factored the post-recall scoring into _apply_recall_scoring but left do_recall's response-metadata loop and recall-count update referencing the now-moved locals time_range_hours / recall_counts, so every recall raised NameError when CONFIDENCE_ENABLED (the production config). _apply_recall_scoring now returns (results, time_range_hours, recall_counts) and do_recall reuses them. Adds a mocked-DB do_recall regression test (test_do_recall_response) that runs without a resident embedding server — the integration recall tests hang in that environment and so did not catch this. No schema change (SCHEMA_VERSION stays 10); tool count unchanged at 24.\nv2.4.27: Extend the recall-precision calibration to the confidence gate (Goal #132 follow-up). v2.4.26 calibrated the post-fusion gate on the rsf/rrf fused score, but _apply_quality_gate evaluates the confidence branch first when CONFIDENCE_ENABLED, so on a confidence-enabled deployment the calibrated gate was bypassed and precision still ran on the pool-size heuristic _adaptive_min_score. The gate calibration now keys on whichever score the gate actually compares — confidence > rsf > cosine > rrf precedence, via _gate_score — and tags the calibrated value with its 'signal'; _apply_quality_gate applies it only to the matching branch, so a gate calibrated under one config (e.g. confidence-on) is simply inert under another with no scale mismatch. The simulate-query calibration now runs the same post-recall scoring as do_recall (episode-boundary penalty + confidence), factored into _apply_recall_scoring, so the operating point is computed on the exact runtime gate score. The startup guard now calibrates the gate when restoring a sidecar that lacks a gate signal (e.g. a v2.4.25 -> v2.4.27 upgrade with unchanged embedding dimension), so the gate activates without waiting for a dimension change. The sidecar stores the gate 'signal' instead of the recall 'mode' (backward compatible — an older sidecar's missing signal triggers a one-time gate calibration at startup). No new tools or env. No schema change (SCHEMA_VERSION stays 10); tool count unchanged at 24.\nv2.4.26: Post-fusion quality-gate calibration (recall precision, Goal #132). The v2.4.24/25 vector-threshold calibration was inert in the rsf/rrf fusion modes: the pre-fusion vector floor is scaled by RRF_THRESHOLD_FACTOR below the null mean, and the post-fusion quality gate used a pool-size heuristic (_adaptive_min_score) that never consulted the calibrated distribution, so fusion-mode precision was uncalibrated (the calibration only bit in cascade). This calibrates the post-fusion gate with the same separation method applied to the fused-score distribution. Because fused scores (_rsf_score / _rrf_score) are query-dependent and cannot be derived from stored embeddings alone, the distributions are produced by simulate-query: at calibration time the server samples stored memories as pseudo-queries, runs the live RECALL_MODE fusion pipeline, and separates the fused scores of temporally-adjacent (same-session) rows from unrelated rows at the Youden-J operating point. The gate is per-agent, keyed by embedding dimension, persisted in the calibration sidecar alongside the vector threshold, and applied only when its calibration mode matches the live RECALL_MODE (same score scale). Adds knob 3, the precision point: CPERSONA_RECALL_PRECISION = strict | balanced (default) | lenient maps to a specificity weight (beta) in the separation objective (sensitivity + beta*specificity), shifting where on the calibrated curve the gate sits — the one policy choice the system cannot derive from data. New env: CPERSONA_RECALL_PRECISION / CPERSONA_FUSED_GATE_ENABLED / CPERSONA_FUSED_GATE_SAMPLE_QUERIES / CPERSONA_FUSED_GATE_BETA. Falls back to the pool-size heuristic when disabled, in cascade mode, or when too few samples exist; a pre-2.4.26 calibration sidecar restores the vector threshold only (backward compatible). No schema change (SCHEMA_VERSION stays 10); tool count unchanged at 24.\nv2.4.25: Autocut small-result hardening. Autocut now keeps every row when the result set is below CPERSONA_AUTOCUT_MIN_RESULTS (default 3). Under RSF, _minmax_norm pins the lowest-scoring row to 0.0, so a 2-item set always carried a full-scale gap that autocut cut to a single row — discarding a still-relevant second hit (the over-cut that blocked making rsf the default). RRF stays the default; rsf remains opt-in. No schema change (SCHEMA_VERSION stays 10); tool count unchanged at 24.\nv2.4.24: Recall-contamination root fix in vector-threshold calibration. The auto-calibration formula had an inverted sign (threshold = mean - z*std), placing the vector-similarity floor BELOW the mean of the random-pair (null) cosine distribution, so most unrelated memories cleared the gate — severe topic drift on anisotropic embedding models (bge-m3 random-pair mean ~0.51). Fixed to mean + z*std and added two methods: 'percentile' (a quantile of the null distribution, distribution-free) and 'separation' (new default) — a two-population operating point maximizing Youden's J between the null and a label-free positive proxy (same-session memories via temporal adjacency, nearest-neighbour fallback), learning the threshold from the corpus's own separability instead of a fixed value. Tier-4 guard: the calibrated threshold is persisted to a sidecar keyed by embedding dimension and restored on restart; an embedding-dimension change (e.g. a silent jina 768d to bge-m3 1024d swap) forces recalibration at startup even when AUTO_CALIBRATE is off — a stale jina-era threshold surviving such a swap was the contamination trigger. New env: CPERSONA_CALIBRATE_METHOD / CPERSONA_CALIBRATE_PERCENTILE / CPERSONA_CALIBRATE_TEMPORAL_WINDOW_MIN / CPERSONA_CALIBRATE_ON_MODEL_CHANGE. calibrate_threshold gains method/percentile args and reports null_admit_rate / youden_j / proxy_source. No schema change (SCHEMA_VERSION stays 10); tool count unchanged at 24.\nv2.4.23: Recall-contamination fixes + Relative Score Fusion (opt-in). (Bug A / bug-001) config.py now reads the generic embedding env keys EMBEDDING_MODE / EMBEDDING_HTTP_URL / EMBEDDING_API_* / EMBEDDING_MODEL with the CPERSONA_* key as an override (specific > generic, matching the marketplace catalog and the CScheduler convention) — a catalog-installed instance, which sets the generic keys, previously ran with embeddings silently off and recall degraded to FTS-only. (Bug B / bug-002) the FTS keyword query is now built by decomposing CJK runs into overlapping trigram OR-terms instead of query.split(), which collapsed space-less scripts (Japanese / Chinese) into one phrase the trigram index never matched — the keyword retriever was dead for Japanese, removing the channel RRF needs to separate topically-distinct memories. New CPERSONA_RECALL_MODE=rsf (Relative Score Fusion): per-query min-max-normalizes each channel raw score (cosine / bm25) and sums, so the keyword channel bm25 magnitude survives the merge that RRF rank-only fusion flattens; rrf stays the default, rsf is opt-in and recommended for topic-drift-prone / Japanese contexts. End-to-end bench (full-precision bge-m3): raspberry contamination 9 -> 2 with rsf and no recall loss. No schema change (SCHEMA_VERSION stays 10); tool count unchanged at 24.\nv2.4.22: Per-channel episodic loop. Episodes gain a channel column (SCHEMA_VERSION 9 -> 10, in-place ALTER backfill) so archive_episode / recall can scope episodes to one conversation channel (e.g. a Discord channel id); empty channel = unscoped / shared.\nv2.4.21: Vendor mcp-common into _vendored_mcp_common to remove the transitive git+url dependency, so a marketplace git-clone install resolves without a separate mcp-common fetch. No schema or tool-surface change.\nv2.4.20: Add a source_id prefix filter to recall (json_extract(source, '$.id') LIKE prefix), letting multi-user sessions (e.g. Discord) scope recall to one user / source. No schema change.\nv2.4.19: Session no-persist mode. Adds three tools (pause_persistence / resume_persistence / persistence_status) that turn this server's write tools into no-ops for an opt-in TTL window (default 30 min, max 1 day). While paused, store / archive_episode / update_* / delete_* / lock_* / unlock_memory / update_profile / import_memories / merge_memories / calibrate_threshold return responses with `persisted: false`, `dry_run: true`, and `id: \"no-persist\"` instead of writing. The MemoryTaskQueue worker drops queued archive_episode / update_profile tasks under no-persist. check_health(fix=true) and deep_check(fix=true) downgrade to fix=False during no-persist and report `repairs_skipped: true`. Read tools (recall, list_*, get_profile, etc.) are unaffected. Module-level state per process; server restart loses the flag. Symmetric with cscheduler 0.2.6 — clients should call pause_persistence on both servers if they want both paused. Tool count: 21 → 24.\nv2.4.18: Internal refactor — γ-semantics SQL builder and project_id coercer moved to common/isolation.py (shared with cscheduler 0.2.4). No behavior, schema, or tool-surface change.\nv2.4.17: agent_id × project_id two-tier isolation (γ semantics). SCHEMA_VERSION 8→9 adds project_id (TEXT NOT NULL DEFAULT '') to memories / episodes / profiles / pending_memory_tasks via ALTER TABLE backfill — existing DBs are upgraded in place. Writes accept project_id (defaults to '' = global pool when omitted). Reads apply γ filter (None = no filter, '' = global pool only, 'X' = 'X' ∪ global pool). store dedup (msg_id / content) is now project-scoped."
}
